package main

import (
	"go-microservices/handlers"
)

var asendiaHandler = new(handlers.AsendiaHandler)

func main() {
	//service := router2.New(":8080", asendiaHandler)
	//router := service.New()
	//log.Fatal(http.ListenAndServe(service.ServerAddress, router))
}